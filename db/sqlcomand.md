
les1

sudo apt-get update

sudo apt-get install mysql-server

sudo apt-get install mysql-client

sudo mysql -u root /VHOD V SQL


HELP
SHOW DATABASES;
CREATE DATABASE mysomfingbase;
USE myb;
CREATE TABLE names (name_id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(20));
SHOW TABLES;

CREATE TABLE names ()

select * from names; /vubrat iz tablicu names

INSERT INTO names VALUES(null,'Gricha');

INSERT INTO names VALUES(null, 'Viktor');

INSERT INTO names VALUES(null, 'EVGEN');

SELECT select * from names  


help create | Header One     | Header Two     |
| :------------- | :------------- |
| Item One       | Item Two       |


help create table


SHOW DATABASES

use mysql

show tables  

help AUTO_INCRIMENT


use my


CREATE TABLE names (name_id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(20));


CREATE TABLE names (name_id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(20));
