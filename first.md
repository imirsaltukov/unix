# Первый урок Начинаем знакомство с версией

git config --global user.name "EvanBaibakov"

git config --global user.email 8090500@mail.ru

git init /  Инициализация проекта

git status / Проверка проекта

git add fail // значит добавили фаил

git commim -m "Komit"// пересохранили фаил с изменениями описав их

git diff  // просмотр изменений и комитов в командной строке что добавили что убрали

git log / просмотр файлов
git log  --all --oneline --graph

git log --graph 

git checkout "номер комита"
git checkout "переключение на ветку" /   

* курсив * ggg

**полужирный

## vetki

git branch new branch
git branch --d

git checkout nazvanie vetki
git switch
git checkout -b nazvanie vetki i srazu perekluchenie

обединение веток

git checkout main
git merge nazvanie vetki

Удаление ветки

git branch -d nazvanie vetki

**end of vetki

# First Second Third

git puch
git pull
git pull reaqwest

Программа работы по запуску продукта на ностинг
Гит хаб
Галп сборщик
Бутстрап адаптация
Сохранение версий в гит хаб
Постоянныекомиты новых сохранений
Ребек на ветки
Начало с простого одностраничного формата

# first file git version

## Komandu

* git --version
* git --init  инициализация гита в папке
* git --status
* git -- add index.html  dobavlenie faila
* git commit -m "Sozdaem novui fail"
* git log
* git checkout   *versia prosmotr*
* git checkout master*

* git diff pokazuvaet raznicu

переключили на ветку три
 git fetich
git checkout / проверка веткипше 

git monesiman limesiman
git checkout / проверка веткипше  limanese

 chusin my confenshon commit

  git log --all -- oneline --graph

            git merge ?
            git pull request
            git push
 
